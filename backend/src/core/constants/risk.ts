/**
 * Initial risk constants used by contracts and configuration defaults.
 * They are guardrails, not executable risk logic.
 */
export const DEFAULT_MAX_RISK_PER_TRADE_PERCENT = 1;

export const DEFAULT_MAX_DAILY_LOSS_PERCENT = 3;

export const DEFAULT_MAX_TOTAL_DRAWDOWN_PERCENT = 10;

export const DEFAULT_MIN_RISK_REWARD_RATIO = 2;

export const MARTINGALE_ALLOWED = false;

export const KILL_SWITCH_DEFAULT_ENABLED = true;

export const EMERGENCY_STOP_DEFAULT_ENABLED = true;
